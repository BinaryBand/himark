"""Phase 1: Split a raw HMK statement into its pattern and template sides."""


def split_statement(text: str) -> tuple[str, str | None]:
    """Split 'pattern => template' into (pattern, template).

    Returns (text, None) if no transformer arrow is present.
    """
    # Placeholder — transformer splitting logic goes here
    return text, None
